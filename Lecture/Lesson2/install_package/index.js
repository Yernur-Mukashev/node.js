const yernurMathPackage = require('yernur_math_package');

const addResult = yernurMathPackage.add(3, 9);
const subResult = yernurMathPackage.subtract(9, 3);

console.log(addResult);
console.log(subResult);